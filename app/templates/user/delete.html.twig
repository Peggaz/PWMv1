{% include 'default/_form.html.twig' with {
    page_title: 'title.user_delete'|trans,
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'DELETE',
        action: url('user_delete', {'id': user.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'user_index',
} %}